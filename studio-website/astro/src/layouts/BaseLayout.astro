---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

export interface Props {
  title?: string;
  description?: string;
  showReservationBanner?: boolean;
  showFixedReservationButton?: boolean;
}

const {
  title = 'スタジオゼブラ | 新宿・代々木エリアの白ホリゾント撮影スタジオ',
  description = '新宿・代々木エリアの白ホリゾント撮影スタジオ。天高3m超・引き9mの白ホリスタジオ。あなたのスタジオワークをもっと自由に、快適に。',
  showReservationBanner = true,
  showFixedReservationButton = true
} = Astro.props;
---

<!DOCTYPE html>
<html lang="ja" class="no-js responsive">
<head>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-9958VF71PZ"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-9958VF71PZ');
  </script>

  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge">
  <meta name="author" content="FOODCONNECTION">

  <!-- Mobile -->
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="format-detection" content="telephone=no, date=no, address=no, email=no">

  <!-- Viewport -->
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=0">

  <!-- SEO -->
  <title>{title}</title>
  <meta name="description" content={description}>

  <!-- Icons (プレースホルダー) -->
  <link rel="apple-touch-icon" href="/favicon.svg">
  <link rel="shortcut icon" href="/favicon.svg">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">

  <!-- Styles -->
  <link rel="stylesheet" href="/styles/global.css">
</head>

<body class="ie-smoothscroll">
  <main>
    <Header />
    <slot />
    <Footer
      showReservationBanner={showReservationBanner}
      showFixedReservationButton={showFixedReservationButton}
    />
  </main>
</body>
</html>
